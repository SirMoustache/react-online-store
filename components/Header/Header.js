/**
 * Absolute imports
 */
import React from 'react';
import styled from 'styled-components';

const Root = styled.header`
  flex: 0 0 auto;
`;

const Header = () => <Root>Header placeholder</Root>;

export default Header;
